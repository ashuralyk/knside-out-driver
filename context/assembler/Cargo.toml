[package]
name = "ko-context-assembler"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ckb-hash = "0.104"

ko-rpc-client = { path = "../../rpc/client" }
ko-protocol = { path = "../../protocol" }
